# FlowTV Environment Configuration
# Copy this file to .env and fill in your values.
# The .env file is gitignored and should never be committed.

# Optional: Override default EPG refresh interval (hours)
# EPG_REFRESH_INTERVAL=6

# Optional: Override default playlist refresh interval (hours)
# PLAYLIST_REFRESH_INTERVAL=24
